import 'dart:io';
import 'dart:math';
main(List args) {
  List numbers = new List();
  numbers.add(3);
  numbers.add(6);
  numbers.add(9);
  numbers.add(8);
  numbers.add(2);
  numbers.add(5);
  numbers.add(1);

  for (var i =0 ; i < numbers.length;i++) {
    
  print("position is ${numbers[i]}");
  }
}
